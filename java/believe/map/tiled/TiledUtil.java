package believe.map.tiled;

/** Utility class with helper methods for dealing with Tiled classes. */
public final class TiledUtil {

  /** Returns true if {@code id} is considered empty. */
  public static boolean isEmptyId(int id) {
    return id == 0;
  }

  private TiledUtil() {}
}
